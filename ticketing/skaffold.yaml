apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: eeki/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .


# Google cloud build
#build:
#  googleCloudBuild:
#    projectId: 'ticketing-dev-324307'
#  artifacts:
#    - image: eu.gcr.io/ticketing-dev-324307/auth
